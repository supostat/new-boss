// Deterministic e2e identity: the global setup creates it idempotently
// through the same CLI a human would use.
export const E2E_ADMIN = {
  email: "e2e-admin@boss.test",
  password: "e2e-sufficiently-long-password",
};
